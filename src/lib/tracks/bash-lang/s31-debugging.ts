import type { LessonSection } from '../../../stores/app.svelte.ts';

export const section: LessonSection = {
  id: 'bash-debug',
  title: '31. Debugging',
  explanation: `## Debugging Bash Scripts\n\nBash provides several built-in debugging tools and options.\n\n\`\`\`bash\n# Trace execution (shows each command before it runs)\nset -x          # Enable trace\nset +x          # Disable trace\nbash -x script.sh  # Run entire script with trace\n\n# Verbose mode (shows lines as they are read)\nset -v          # Enable verbose\nbash -v script.sh\n\n# Customize trace prompt\nPS4='+ \\\${BASH_SOURCE}:\\\${LINENO}: '\nset -x\n\n# Syntax check (no execution)\nbash -n script.sh\n\n# Strict mode options\nset -e          # Exit on any error (errexit)\nset -u          # Error on undefined variables (nounset)\nset -o pipefail # Pipe fails if any command fails\n\n# DEBUG trap (runs before every command)\ntrap 'echo "DEBUG: \\\$BASH_COMMAND"' DEBUG\n\n# Useful debug variables\necho \\\$BASH_SOURCE  # Current script file\necho \\\$LINENO       # Current line number\necho \\\$FUNCNAME     # Current function name\necho \\\$BASH_COMMAND # Command about to execute\n\n# Conditional debugging\n[[ \\\${DEBUG:-0} -eq 1 ]] && set -x\n\`\`\`\n\n**Debugging tools:**\n- **set -x**: See what commands actually run\n- **set -v**: See the raw script lines\n- **PS4**: Customize the trace prefix\n- **bash -n**: Check syntax without running\n- **trap DEBUG**: Hook into every command\n- **shellcheck**: External linting tool`,
  exercises: [
    {
      id: 'bash-debug-1',
      title: 'Enable Execution Trace',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Enable execution tracing so each command is printed before it runs.',
      skeleton: `#!/bin/bash\n___ -x\nname="Alice"\necho "Hello, \$name"`,
      solution: `#!/bin/bash\nset -x\nname="Alice"\necho "Hello, \$name"`,
      hints: ['The set command controls shell options.', '-x enables execution tracing.', 'set -x shows commands as they execute.'],
      concepts: ['set', 'xtrace'],
    },
    {
      id: 'bash-debug-2',
      title: 'Disable Trace Temporarily',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Disable tracing for the password assignment, then re-enable it.',
      skeleton: `#!/bin/bash\nset -x\nuser="admin"\n___ +x\npassword="secret123"\n___ -x\necho "User: \$user"`,
      solution: `#!/bin/bash\nset -x\nuser="admin"\nset +x\npassword="secret123"\nset -x\necho "User: \$user"`,
      hints: ['set +x disables tracing.', 'Use + to disable and - to enable options.', 'Surround sensitive lines with set +x / set -x.'],
      concepts: ['set', 'xtrace', 'security'],
    },
    {
      id: 'bash-debug-3',
      title: 'Customize PS4 Trace Prompt',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Set PS4 to show the filename and line number in trace output.',
      skeleton: `#!/bin/bash\n___='+ \${BASH_SOURCE}:\${LINENO}: '\nset -x\necho "hello"`,
      solution: `#!/bin/bash\nPS4='+ \${BASH_SOURCE}:\${LINENO}: '\nset -x\necho "hello"`,
      hints: ['PS4 is the trace prompt prefix.', 'It supports variable expansion.', 'BASH_SOURCE and LINENO are built-in variables.'],
      concepts: ['PS4', 'trace-prompt'],
    },
    {
      id: 'bash-debug-4',
      title: 'Syntax Check Only',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Write the command to check a script for syntax errors without executing it.',
      skeleton: `___ script.sh`,
      solution: `bash -n script.sh`,
      hints: ['bash -n checks syntax without running.', 'The -n flag stands for noexec.', 'bash -n script.sh validates syntax.'],
      concepts: ['syntax-check', 'bash-n'],
    },
    {
      id: 'bash-debug-5',
      title: 'Error on Undefined Variable',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Enable the option that causes the script to error when using an undefined variable.',
      skeleton: `#!/bin/bash\nset ___\necho "\$undefined_var"`,
      solution: `#!/bin/bash\nset -u\necho "\$undefined_var"`,
      hints: ['The -u option is "nounset".', 'set -u errors on undefined variables.', 'Without -u, undefined variables silently expand to empty.'],
      concepts: ['set', 'nounset'],
    },
    {
      id: 'bash-debug-6',
      title: 'Enable pipefail',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Enable pipefail so the pipeline returns the exit code of the first failing command.',
      skeleton: `#!/bin/bash\nset -o ___\nfalse | true\necho "Exit: \$?"`,
      solution: `#!/bin/bash\nset -o pipefail\nfalse | true\necho "Exit: \$?"`,
      hints: ['pipefail changes pipeline exit code behavior.', 'set -o pipefail enables it.', 'Without pipefail, only the last command exit code matters.'],
      concepts: ['pipefail', 'set'],
    },
    {
      id: 'bash-debug-7',
      title: 'Write a DEBUG Trap',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a DEBUG trap that prints the current line number and command before each command executes.',
      skeleton: `#!/bin/bash\n# Set up DEBUG trap\n\necho "step 1"\necho "step 2"`,
      solution: `#!/bin/bash\ntrap 'echo "LINE \$LINENO: \$BASH_COMMAND"' DEBUG\n\necho "step 1"\necho "step 2"`,
      hints: ['trap DEBUG runs before every command.', '$LINENO is the current line number.', '$BASH_COMMAND is the command about to execute.'],
      concepts: ['trap', 'DEBUG', 'LINENO'],
    },
    {
      id: 'bash-debug-8',
      title: 'Write Conditional Debug Mode',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a script that enables xtrace only when the DEBUG environment variable is set to 1.',
      skeleton: `#!/bin/bash\n# Enable debug mode conditionally\n\necho "Running..."`,
      solution: `#!/bin/bash\n[[ "\${DEBUG:-0}" -eq 1 ]] && set -x\n\necho "Running..."`,
      hints: ['Use ${DEBUG:-0} for a safe default.', 'Check if it equals 1 with [[ ]].', 'Use && to conditionally enable set -x.'],
      concepts: ['conditional-debug', 'environment-variables'],
    },
    {
      id: 'bash-debug-9',
      title: 'Write a Debug Log Function',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a debug() function that only prints messages when VERBOSE=1. It should prefix messages with "DEBUG:" and include the calling function name.',
      skeleton: `#!/bin/bash\nVERBOSE=1\n# Write the debug function`,
      solution: `#!/bin/bash\nVERBOSE=1\ndebug() {\n  [[ "\${VERBOSE:-0}" -eq 1 ]] && echo "DEBUG [\${FUNCNAME[1]}]: \$*" >&2\n}`,
      hints: ['FUNCNAME[1] gives the calling function name.', 'Send debug output to stderr with >&2.', 'Check VERBOSE before printing.'],
      concepts: ['debug-function', 'FUNCNAME', 'stderr'],
    },
    {
      id: 'bash-debug-10',
      title: 'Write a Stack Trace Function',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a function called stacktrace that prints the call stack using FUNCNAME and BASH_LINENO arrays.',
      skeleton: `#!/bin/bash\n# Write stacktrace function`,
      solution: `#!/bin/bash\nstacktrace() {\n  local i\n  echo "Stack trace:" >&2\n  for ((i=1; i<\${#FUNCNAME[@]}; i++)); do\n    echo "  [\$i] \${FUNCNAME[\$i]}() at line \${BASH_LINENO[\$((i-1))]}" >&2\n  done\n}`,
      hints: ['FUNCNAME is an array of function call names.', 'BASH_LINENO has matching line numbers.', 'Start from index 1 to skip stacktrace itself.'],
      concepts: ['FUNCNAME', 'BASH_LINENO', 'stack-trace'],
    },
    {
      id: 'bash-debug-11',
      title: 'Write Timed Execution Debug',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a script that uses PS4 with SECONDS to show elapsed time in trace output, then enable xtrace.',
      skeleton: `#!/bin/bash\n# Set up timed trace output`,
      solution: `#!/bin/bash\nPS4='+ [\${SECONDS}s] '\nset -x\necho "step 1"\nsleep 1\necho "step 2"`,
      hints: ['SECONDS tracks time since script start.', 'Include SECONDS in PS4 for timing.', 'PS4 is evaluated each time a trace line is printed.'],
      concepts: ['PS4', 'SECONDS', 'timing'],
    },
    {
      id: 'bash-debug-12',
      title: 'Write Function Entry/Exit Logger',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a script with a RETURN trap that logs when functions exit, and a DEBUG trap that logs function entry. Define a test function called process_data.',
      skeleton: `#!/bin/bash\n# Write traps for function entry/exit logging`,
      solution: `#!/bin/bash\ntrap 'echo "ENTER: \${FUNCNAME[0]} (line \$LINENO)"' DEBUG\ntrap 'echo "EXIT: \${FUNCNAME[0]}"' RETURN\n\nprocess_data() {\n  echo "Processing..."\n}\nprocess_data`,
      hints: ['DEBUG trap fires before each command.', 'RETURN trap fires when a function returns.', 'FUNCNAME[0] gives the current function name.'],
      concepts: ['DEBUG', 'RETURN', 'trap', 'function-tracing'],
    },
    {
      id: 'bash-debug-13',
      title: 'Predict set -u Output',
      type: 'predict-output',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'What happens when this script runs?',
      skeleton: `#!/bin/bash\nset -u\necho "\$DEFINED_VAR"`,
      solution: `bash: DEFINED_VAR: unbound variable`,
      hints: ['set -u makes undefined variable access an error.', 'DEFINED_VAR was never assigned.', 'The script exits with an error message.'],
      concepts: ['nounset', 'unbound-variable'],
    },
    {
      id: 'bash-debug-14',
      title: 'Predict pipefail Exit Code',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'What does $? show after this pipeline with pipefail enabled?',
      skeleton: `#!/bin/bash\nset -o pipefail\nfalse | true | true\necho \$?`,
      solution: `1`,
      hints: ['pipefail returns the rightmost non-zero exit code.', 'false exits with 1.', 'Without pipefail, $? would be 0 (from the last true).'],
      concepts: ['pipefail', 'exit-codes'],
    },
    {
      id: 'bash-debug-15',
      title: 'Predict BASH_SOURCE Output',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'If this script is saved as /tmp/test.sh and run with "bash /tmp/test.sh", what does it print?',
      skeleton: `#!/bin/bash\necho "\${BASH_SOURCE[0]}"`,
      solution: `/tmp/test.sh`,
      hints: ['BASH_SOURCE[0] is the current script path.', 'It reflects how the script was invoked.', 'It prints the path used to run the script.'],
      concepts: ['BASH_SOURCE'],
    },
    {
      id: 'bash-debug-16',
      title: 'Fix set -e with Intentional Failure',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'This script exits prematurely because grep returns 1 when no match is found. Fix it so the script continues.',
      skeleton: `#!/bin/bash\nset -e\ncount=\$(grep -c "error" logfile.txt)\necho "Errors found: \$count"`,
      solution: `#!/bin/bash\nset -e\ncount=\$(grep -c "error" logfile.txt || true)\necho "Errors found: \$count"`,
      hints: ['grep returns 1 when no matches are found.', 'set -e exits on any non-zero return.', 'Use || true to suppress the non-zero exit.'],
      concepts: ['errexit', 'grep', 'error-suppression'],
    },
    {
      id: 'bash-debug-17',
      title: 'Fix Undefined Variable Error',
      type: 'fix-bug',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Fix this script so it does not crash from set -u when $OPTIONAL_FLAG might not be set.',
      skeleton: `#!/bin/bash\nset -u\nif [ "\$OPTIONAL_FLAG" = "true" ]; then\n  echo "Flag is set"\nfi`,
      solution: `#!/bin/bash\nset -u\nif [ "\${OPTIONAL_FLAG:-}" = "true" ]; then\n  echo "Flag is set"\nfi`,
      hints: ['${var:-} provides an empty default for unset variables.', 'This satisfies set -u without changing behavior.', 'The :- with empty default is the standard pattern.'],
      concepts: ['nounset', 'default-value'],
    },
    {
      id: 'bash-debug-18',
      title: 'Fix Verbose vs Trace Confusion',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'This script uses set -v (verbose) but the developer wants trace mode (showing expanded commands). Fix it.',
      skeleton: `#!/bin/bash\nset -v\nname="Alice"\necho "Hello, \$name"`,
      solution: `#!/bin/bash\nset -x\nname="Alice"\necho "Hello, \$name"`,
      hints: ['-v shows lines as read (before expansion).', '-x shows lines as executed (after expansion).', 'Replace -v with -x for trace mode.'],
      concepts: ['verbose', 'xtrace'],
    },
    {
      id: 'bash-debug-19',
      title: 'Refactor Manual Debug Prints',
      type: 'refactor',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Refactor these scattered debug echo statements to use set -x tracing instead.',
      skeleton: `#!/bin/bash\necho "[DEBUG] Setting name"\nname="Alice"\necho "[DEBUG] Name is \$name"\necho "[DEBUG] Setting greeting"\ngreeting="Hello, \$name"\necho "[DEBUG] Greeting is \$greeting"\necho "\$greeting"`,
      solution: `#!/bin/bash\nset -x\nname="Alice"\ngreeting="Hello, \$name"\nset +x\necho "\$greeting"`,
      hints: ['set -x automatically shows each command.', 'Remove manual debug echo statements.', 'Turn off tracing with set +x before final output.'],
      concepts: ['xtrace', 'refactoring'],
    },
    {
      id: 'bash-debug-20',
      title: 'Refactor to Strict Mode',
      type: 'refactor',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Refactor this script to use strict mode (set -euo pipefail) and fix the code to work with it.',
      skeleton: `#!/bin/bash\nresult=\$(cat missing_file.txt 2>/dev/null)\ncount=\$(echo "\$data" | wc -l)\nif [ "\$VERBOSE" = "1" ]; then\n  echo "Lines: \$count"\nfi\necho "Done"`,
      solution: `#!/bin/bash\nset -euo pipefail\nresult=\$(cat missing_file.txt 2>/dev/null || true)\ncount=\$(echo "\${data:-}" | wc -l)\nif [ "\${VERBOSE:-}" = "1" ]; then\n  echo "Lines: \$count"\nfi\necho "Done"`,
      hints: ['set -e requires handling expected failures with || true.', 'set -u requires defaults for optional variables.', 'Use ${var:-} for variables that might not be set.'],
      concepts: ['strict-mode', 'errexit', 'nounset', 'pipefail'],
    },
  ]
};
