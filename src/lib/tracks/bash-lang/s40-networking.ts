import type { LessonSection } from '../../../stores/app.svelte.ts';

export const section: LessonSection = {
  id: 'bash-net',
  title: '40. Networking',
  explanation: `## Networking in Bash\n\nBash has built-in and external tools for network operations:\n\n\`\`\`bash\n# /dev/tcp and /dev/udp - Bash built-in networking\nexec 3<>/dev/tcp/example.com/80\necho -e "GET / HTTP/1.1\\r\\nHost: example.com\\r\\n\\r\\n" >&3\ncat <&3\nexec 3>&-\n\`\`\`\n\n**Key tools:**\n\n- **/dev/tcp/HOST/PORT** and **/dev/udp/HOST/PORT** - Bash pseudo-devices\n- **curl** - transfer data from/to URLs, supports HTTP, HTTPS, FTP, etc.\n- **wget** - download files from the web\n- **nc (netcat)** - TCP/UDP connections, port scanning, simple servers\n- **ssh** - secure remote command execution\n- **scp/sftp** - secure file transfer\n- **ping** - test host reachability\n- **dig/nslookup** - DNS queries\n- **traceroute** - trace network path\n- **ss/netstat** - socket statistics\n\n\`\`\`bash\n# curl examples\ncurl -s https://api.example.com/data           # silent GET\ncurl -X POST -d '{\"key\":\"val\"}' -H \"Content-Type: application/json\" URL\ncurl -o output.file URL                        # save to file\n\n# Port check with /dev/tcp\nif (echo >/dev/tcp/localhost/8080) 2>/dev/null; then\n  echo \"Port 8080 is open\"\nfi\n\`\`\``,
  exercises: [
    {
      id: 'bash-net-1',
      title: 'Check If a Port Is Open',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Use Bash /dev/tcp to check if port 80 is open on localhost.',
      skeleton: `#!/bin/bash\nif (echo >/dev/___/localhost/80) 2>/dev/null; then\n  echo "open"\nfi`,
      solution: `#!/bin/bash\nif (echo >/dev/tcp/localhost/80) 2>/dev/null; then\n  echo "open"\nfi`,
      hints: ['/dev/tcp is the Bash pseudo-device for TCP.', 'Syntax: /dev/tcp/HOST/PORT.', '/dev/tcp/localhost/80'],
      concepts: ['dev-tcp', 'port-checking'],
    },
    {
      id: 'bash-net-2',
      title: 'Curl GET Request',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Use curl in silent mode to GET data from https://api.example.com/status.',
      skeleton: `#!/bin/bash\n___ -s https://api.example.com/status`,
      solution: `#!/bin/bash\ncurl -s https://api.example.com/status`,
      hints: ['curl transfers data from URLs.', '-s makes curl silent (no progress bar).', 'curl -s URL'],
      concepts: ['curl', 'http-get'],
    },
    {
      id: 'bash-net-3',
      title: 'Save curl Output to File',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Download a file using curl and save it as output.tar.gz.',
      skeleton: `#!/bin/bash\ncurl ___ output.tar.gz https://example.com/file.tar.gz`,
      solution: `#!/bin/bash\ncurl -o output.tar.gz https://example.com/file.tar.gz`,
      hints: ['-o specifies the output filename.', 'Syntax: curl -o FILENAME URL.', 'curl -o output.tar.gz URL'],
      concepts: ['curl', 'download'],
    },
    {
      id: 'bash-net-4',
      title: 'Wget Download',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Use wget to quietly download a file from a URL.',
      skeleton: `#!/bin/bash\n___ -q https://example.com/data.csv`,
      solution: `#!/bin/bash\nwget -q https://example.com/data.csv`,
      hints: ['wget downloads files from the web.', '-q makes wget quiet.', 'wget -q URL'],
      concepts: ['wget', 'download'],
    },
    {
      id: 'bash-net-5',
      title: 'Curl POST with JSON',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Send a POST request with JSON body using curl.',
      skeleton: `#!/bin/bash\ncurl -s -X ___ \\\n  -H "Content-Type: application/json" \\\n  -d '{"name":"test"}' \\\n  https://api.example.com/items`,
      solution: `#!/bin/bash\ncurl -s -X POST \\\n  -H "Content-Type: application/json" \\\n  -d '{"name":"test"}' \\\n  https://api.example.com/items`,
      hints: ['-X sets the HTTP method.', 'POST is used to create resources.', 'curl -s -X POST'],
      concepts: ['curl', 'http-post', 'json'],
    },
    {
      id: 'bash-net-6',
      title: 'DNS Lookup with dig',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Use dig to look up the A record for example.com with short output.',
      skeleton: `#!/bin/bash\n___ +short example.com A`,
      solution: `#!/bin/bash\ndig +short example.com A`,
      hints: ['dig queries DNS servers.', '+short gives concise output.', 'dig +short DOMAIN RECORD_TYPE'],
      concepts: ['dig', 'dns'],
    },
    {
      id: 'bash-net-7',
      title: 'Write a Port Scanner',
      type: 'write-function',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Write a function scan_ports that checks if ports 22, 80, and 443 are open on a given host using /dev/tcp.',
      skeleton: `#!/bin/bash\n# Write scan_ports function`,
      solution: `#!/bin/bash\nscan_ports() {\n  local host=\$1\n  for port in 22 80 443; do\n    if (echo >/dev/tcp/\$host/\$port) 2>/dev/null; then\n      echo "\$port open"\n    else\n      echo "\$port closed"\n    fi\n  done\n}`,
      hints: ['Loop over ports 22 80 443.', 'Use /dev/tcp/$host/$port to test.', 'Redirect stderr to /dev/null for failed connections.'],
      concepts: ['dev-tcp', 'port-scanning', 'functions'],
    },
    {
      id: 'bash-net-8',
      title: 'Write a REST API Caller',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function api_call that takes method, URL, and optional JSON data, and makes a curl request returning the response body. Include proper headers.',
      skeleton: `#!/bin/bash\n# Write api_call function`,
      solution: `#!/bin/bash\napi_call() {\n  local method=\$1\n  local url=\$2\n  local data=\$3\n  if [[ -n "\$data" ]]; then\n    curl -s -X "\$method" \\\n      -H "Content-Type: application/json" \\\n      -d "\$data" "\$url"\n  else\n    curl -s -X "\$method" "\$url"\n  fi\n}`,
      hints: ['Accept method, url, and optional data parameters.', 'Only include -d and Content-Type when data is provided.', 'Use -s for silent mode.'],
      concepts: ['curl', 'rest-api', 'functions'],
    },
    {
      id: 'bash-net-9',
      title: 'Write an HTTP Health Checker',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function check_health that takes a URL, makes a request, and prints "UP" if HTTP status is 200, "DOWN" otherwise. Use curl -o /dev/null -w to get status code.',
      skeleton: `#!/bin/bash\n# Write check_health function`,
      solution: `#!/bin/bash\ncheck_health() {\n  local url=\$1\n  local status\n  status=\$(curl -s -o /dev/null -w "%{http_code}" "\$url")\n  if [[ "\$status" == "200" ]]; then\n    echo "UP"\n  else\n    echo "DOWN"\n  fi\n}`,
      hints: ['curl -w "%{http_code}" outputs the HTTP status code.', '-o /dev/null discards the response body.', 'Compare the status code to "200".'],
      concepts: ['curl', 'http-status', 'health-check'],
    },
    {
      id: 'bash-net-10',
      title: 'Write SSH Remote Command Executor',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function remote_exec that takes a host and a command, runs the command via ssh, and returns the output.',
      skeleton: `#!/bin/bash\n# Write remote_exec function`,
      solution: `#!/bin/bash\nremote_exec() {\n  local host=\$1\n  shift\n  ssh -o ConnectTimeout=5 -o StrictHostKeyChecking=no "\$host" "\$@"\n}`,
      hints: ['Use ssh HOST COMMAND to run remote commands.', '-o ConnectTimeout=5 prevents hanging.', 'shift removes the first arg so $@ has the command.'],
      concepts: ['ssh', 'remote-execution', 'functions'],
    },
    {
      id: 'bash-net-11',
      title: 'Write a Download with Retry',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a function download_retry that takes a URL and output file. Retry up to 3 times with a 2-second delay between attempts. Return 0 on success, 1 on failure.',
      skeleton: `#!/bin/bash\n# Write download_retry function`,
      solution: `#!/bin/bash\ndownload_retry() {\n  local url=\$1\n  local output=\$2\n  local attempt=0\n  while (( attempt < 3 )); do\n    if curl -sf -o "\$output" "\$url"; then\n      return 0\n    fi\n    (( attempt++ ))\n    sleep 2\n  done\n  return 1\n}`,
      hints: ['Use a while loop with an attempt counter.', 'curl -f fails silently on server errors.', 'sleep 2 between attempts.'],
      concepts: ['curl', 'retry-logic', 'functions'],
    },
    {
      id: 'bash-net-12',
      title: 'Write a Netcat Listener Script',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a function start_listener that uses nc to listen on a given port and echo received data. Accept port as parameter.',
      skeleton: `#!/bin/bash\n# Write start_listener function`,
      solution: `#!/bin/bash\nstart_listener() {\n  local port=\$1\n  echo "Listening on port \$port..."\n  nc -l -p "\$port" | while read -r line; do\n    echo "Received: \$line"\n  done\n}`,
      hints: ['nc -l -p PORT listens on a port.', 'Pipe nc output to a while-read loop.', 'Echo each received line with a prefix.'],
      concepts: ['netcat', 'tcp-listener', 'functions'],
    },
    {
      id: 'bash-net-13',
      title: 'Predict curl Output',
      type: 'predict-output',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'What does this curl command output?',
      skeleton: `#!/bin/bash\necho "HTTP/1.1 200 OK" | head -1 | cut -d' ' -f2`,
      solution: `200`,
      hints: ['head -1 takes the first line.', 'cut splits by space delimiter.', '-f2 takes the second field: "200".'],
      concepts: ['curl', 'text-processing'],
    },
    {
      id: 'bash-net-14',
      title: 'Predict /dev/tcp Behavior',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'What is the exit code when /dev/tcp connection fails?',
      skeleton: `#!/bin/bash\n(echo >/dev/tcp/192.0.2.1/99999) 2>/dev/null\necho \$?`,
      solution: `1`,
      hints: ['A failed /dev/tcp connection returns non-zero.', 'Port 99999 is invalid (max is 65535).', 'The exit code for failure is 1.'],
      concepts: ['dev-tcp', 'exit-codes'],
    },
    {
      id: 'bash-net-15',
      title: 'Predict HTTP Method',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'What HTTP method does this curl command use?',
      skeleton: `#!/bin/bash\nmethod="-X DELETE"\necho "\$method" | awk '{print \$2}'`,
      solution: `DELETE`,
      hints: ['awk prints the second field.', 'The string is "-X DELETE".', 'Field 2 is DELETE.'],
      concepts: ['curl', 'http-methods'],
    },
    {
      id: 'bash-net-16',
      title: 'Fix Missing curl Flag',
      type: 'fix-bug',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Fix this curl command that shows a progress bar when it should be silent.',
      skeleton: `#!/bin/bash\nresponse=\$(curl https://api.example.com/data)\necho "\$response"`,
      solution: `#!/bin/bash\nresponse=\$(curl -s https://api.example.com/data)\necho "\$response"`,
      hints: ['curl shows progress by default.', 'The -s flag makes curl silent.', 'Add -s to suppress progress output.'],
      concepts: ['curl', 'silent-mode'],
    },
    {
      id: 'bash-net-17',
      title: 'Fix wget Overwrite Issue',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Fix this wget command that creates numbered duplicates instead of overwriting the file.',
      skeleton: `#!/bin/bash\nwget -q https://example.com/config.json\nwget -q https://example.com/config.json`,
      solution: `#!/bin/bash\nwget -q -O config.json https://example.com/config.json\nwget -q -O config.json https://example.com/config.json`,
      hints: ['wget creates .1, .2, etc. by default.', '-O specifies the output filename explicitly.', 'Use -O config.json to always overwrite.'],
      concepts: ['wget', 'output-file'],
    },
    {
      id: 'bash-net-18',
      title: 'Fix SSH Hanging Script',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Fix this script that hangs when the SSH host is unreachable.',
      skeleton: `#!/bin/bash\nresult=\$(ssh user@unreachable-host "uptime")\necho "\$result"`,
      solution: `#!/bin/bash\nresult=\$(ssh -o ConnectTimeout=5 user@unreachable-host "uptime" 2>/dev/null)\necho "\$result"`,
      hints: ['SSH waits indefinitely by default.', '-o ConnectTimeout=N sets a timeout in seconds.', 'Also redirect stderr to /dev/null for clean output.'],
      concepts: ['ssh', 'timeout', 'error-handling'],
    },
    {
      id: 'bash-net-19',
      title: 'Refactor Multiple curl Calls',
      type: 'refactor',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Refactor these repetitive curl calls into a loop over an array of endpoints.',
      skeleton: `#!/bin/bash\ncurl -s https://api.example.com/users > users.json\ncurl -s https://api.example.com/posts > posts.json\ncurl -s https://api.example.com/comments > comments.json`,
      solution: `#!/bin/bash\nendpoints=(users posts comments)\nfor ep in "\${endpoints[@]}"; do\n  curl -s "https://api.example.com/\$ep" > "\$ep.json"\ndone`,
      hints: ['Store endpoint names in an array.', 'Loop over the array to make each call.', 'Use the endpoint name for both URL and filename.'],
      concepts: ['curl', 'arrays', 'loops', 'refactoring'],
    },
    {
      id: 'bash-net-20',
      title: 'Refactor Inline Networking to Functions',
      type: 'refactor',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Refactor this inline networking code into reusable http_get and http_post functions.',
      skeleton: `#!/bin/bash\n# Get user\nuser=\$(curl -s -H "Authorization: Bearer \$TOKEN" https://api.example.com/user)\necho "\$user"\n# Create item\nresult=\$(curl -s -X POST -H "Authorization: Bearer \$TOKEN" -H "Content-Type: application/json" -d '{"name":"item1"}' https://api.example.com/items)\necho "\$result"`,
      solution: `#!/bin/bash\nhttp_get() {\n  curl -s -H "Authorization: Bearer \$TOKEN" "\$1"\n}\n\nhttp_post() {\n  curl -s -X POST \\\n    -H "Authorization: Bearer \$TOKEN" \\\n    -H "Content-Type: application/json" \\\n    -d "\$2" "\$1"\n}\n\nuser=\$(http_get "https://api.example.com/user")\necho "\$user"\nresult=\$(http_post "https://api.example.com/items" '{"name":"item1"}')\necho "\$result"`,
      hints: ['Extract common curl patterns into functions.', 'http_get takes a URL, http_post takes URL and data.', 'Move the auth header into the functions.'],
      concepts: ['curl', 'functions', 'refactoring', 'dry'],
    },
  ]
};
