import type { LessonSection } from '../../../stores/app.svelte.ts';

export const section: LessonSection = {
  id: 'bash-exit',
  title: '15. Exit Codes',
  explanation: `## Exit Codes in Bash\n\nEvery command returns an **exit code** (0-255). By convention, **0 means success** and non-zero means failure:\n\n\`\`\`bash\ntrue\necho \\\$?   # 0\n\nfalse\necho \\\$?   # 1\n\`\`\`\n\n**exit** terminates a script; **return** exits a function:\n\n\`\`\`bash\nexit 0    # success\nexit 1    # general error\nreturn 0  # from a function\n\`\`\`\n\n**Standard exit codes:**\n- 0: Success\n- 1: General error\n- 2: Misuse of builtin\n- 126: Command not executable\n- 127: Command not found\n- 128+N: Killed by signal N\n\n**set -e** makes the script exit on any command failure:\n\n\`\`\`bash\nset -e\nfalse      # script exits here\necho "never reached"\n\`\`\`\n\n**Chaining** with && and ||:\n\n\`\`\`bash\nmkdir /tmp/test && echo "created"   # runs echo only if mkdir succeeds\nmkdir /root/test || echo "failed"   # runs echo only if mkdir fails\n\`\`\``,
  exercises: [
    {
      id: 'bash-exit-1',
      title: 'Check Exit Code',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Print the exit code of the previous command.',
      skeleton: `ls /nonexistent 2>/dev/null\necho "Exit code: ___"`,
      solution: `ls /nonexistent 2>/dev/null\necho "Exit code: \\\$?"`,
      hints: ['$? holds the exit code of the last command.', 'It is a special variable.', '\\$?'],
      concepts: ['exit-code', 'special-variables'],
    },
    {
      id: 'bash-exit-2',
      title: 'Exit with Success',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Exit a script with a success code.',
      skeleton: `echo "All done"\n___ ___`,
      solution: `echo "All done"\nexit 0`,
      hints: ['exit terminates the script.', '0 means success.', 'exit 0'],
      concepts: ['exit-code', 'exit-command'],
    },
    {
      id: 'bash-exit-3',
      title: 'Return from Function',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Use return to exit a function with a specific code.',
      skeleton: `is_root() {\n    if [ "\\\$(id -u)" -eq 0 ]; then\n        ___ 0\n    else\n        ___ 1\n    fi\n}`,
      solution: `is_root() {\n    if [ "\\\$(id -u)" -eq 0 ]; then\n        return 0\n    else\n        return 1\n    fi\n}`,
      hints: ['return exits a function, not the script.', 'return 0 for success, return 1 for failure.', 'return'],
      concepts: ['return', 'function', 'exit-code'],
    },
    {
      id: 'bash-exit-4',
      title: 'Predict Exit Code',
      type: 'predict-output',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Predict the exit code after a successful command.',
      skeleton: `echo "hello" > /dev/null\necho \\\$?`,
      solution: `0`,
      hints: ['echo succeeds.', 'Redirecting to /dev/null does not cause an error.', '0 means success.'],
      concepts: ['exit-code'],
    },
    {
      id: 'bash-exit-5',
      title: 'AND Chaining',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Chain two commands so the second only runs if the first succeeds.',
      skeleton: `mkdir -p /tmp/mydir ___ echo "Directory created"`,
      solution: `mkdir -p /tmp/mydir && echo "Directory created"`,
      hints: ['&& runs the second command only if the first succeeds.', 'It is a logical AND.', '&&'],
      concepts: ['and-chaining', 'exit-code'],
    },
    {
      id: 'bash-exit-6',
      title: 'OR Chaining',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Provide a fallback command when the first one fails.',
      skeleton: `cd /nonexistent ___ echo "Directory not found"`,
      solution: `cd /nonexistent || echo "Directory not found"`,
      hints: ['|| runs the second command only if the first fails.', 'It is a logical OR.', '||'],
      concepts: ['or-chaining', 'exit-code'],
    },
    {
      id: 'bash-exit-7',
      title: 'Predict AND/OR Chain Output',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Predict output of combined && and || operators.',
      skeleton: `false && echo "yes" || echo "no"`,
      solution: `no`,
      hints: ['false fails, so && skips echo "yes".', 'Since the left side of || failed, echo "no" runs.', 'Only "no" is printed.'],
      concepts: ['and-chaining', 'or-chaining'],
    },
    {
      id: 'bash-exit-8',
      title: 'Write Function with Exit Codes',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function check_file that returns 0 if a file exists and is readable, 1 if it exists but is not readable, and 2 if it does not exist.',
      skeleton: `# Write check_file FILEPATH\n# Return 0=readable, 1=not readable, 2=not found`,
      solution: `check_file() {\n    if [ ! -e "\\\$1" ]; then\n        return 2\n    elif [ ! -r "\\\$1" ]; then\n        return 1\n    else\n        return 0\n    fi\n}`,
      hints: ['Use -e to check existence, -r to check readability.', 'return with different codes for each case.', 'Test existence first, then readability.'],
      concepts: ['exit-code', 'function', 'file-test'],
    },
    {
      id: 'bash-exit-9',
      title: 'Enable set -e',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Enable errexit so the script exits on any command failure.',
      skeleton: `#!/bin/bash\n___ ___\n\necho "starting"\nfalse\necho "this should not print"`,
      solution: `#!/bin/bash\nset -e\n\necho "starting"\nfalse\necho "this should not print"`,
      hints: ['set -e enables errexit.', 'The script will exit when false runs.', 'set -e'],
      concepts: ['set-e', 'errexit'],
    },
    {
      id: 'bash-exit-10',
      title: 'Trap ERR',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a script that traps ERR to print the failing line number before exiting.',
      skeleton: `# Trap ERR to print "Error on line N" and exit 1`,
      solution: `trap 'echo "Error on line \\\${LINENO}"; exit 1' ERR\n\necho "step 1"\nfalse\necho "step 2"`,
      hints: ['trap COMMAND ERR fires on any command failure.', 'LINENO gives the current line number.', 'trap ... ERR'],
      concepts: ['trap', 'err-signal', 'lineno'],
    },
    {
      id: 'bash-exit-11',
      title: 'Fix Missing Error Check',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Fix the script that continues after a critical failure.',
      skeleton: `#!/bin/bash\ncp important.dat backup.dat\nrm important.dat\necho "Backup complete"`,
      solution: `#!/bin/bash\ncp important.dat backup.dat || { echo "Copy failed"; exit 1; }\nrm important.dat\necho "Backup complete"`,
      hints: ['If cp fails, rm will delete the only copy.', 'Check if cp succeeded before removing.', 'Use || to handle the failure case.'],
      concepts: ['exit-code', 'error-handling'],
    },
    {
      id: 'bash-exit-12',
      title: 'Write Die Function',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a die function that prints a message to stderr and exits with a given code.',
      skeleton: `# Write die MESSAGE [EXIT_CODE]\n# Default exit code is 1`,
      solution: `die() {\n    echo "\\\$1" >&2\n    exit "\\\${2:-1}"\n}`,
      hints: ['Print to stderr with >&2.', 'Use ${2:-1} for default exit code.', 'exit with the code.'],
      concepts: ['function', 'exit-code', 'stderr'],
    },
    {
      id: 'bash-exit-13',
      title: 'Predict set -e Behavior',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Predict which lines are printed under set -e.',
      skeleton: `set -e\necho "A"\ntrue\necho "B"\nfalse\necho "C"`,
      solution: `A\nB`,
      hints: ['set -e exits on first failure.', 'false causes the script to exit.', 'echo "C" is never reached.'],
      concepts: ['set-e', 'errexit'],
    },
    {
      id: 'bash-exit-14',
      title: 'Custom Exit Codes',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a validate_age function that returns custom exit codes: 0 for valid (1-150), 3 for too low, 4 for too high, 5 for non-numeric.',
      skeleton: `# Write validate_age AGE\n# Return 0=valid, 3=too low, 4=too high, 5=non-numeric`,
      solution: `validate_age() {\n    local age="\\\$1"\n    if ! [[ "\\\${age}" =~ ^[0-9]+\\\$ ]]; then\n        return 5\n    elif [ "\\\${age}" -lt 1 ]; then\n        return 3\n    elif [ "\\\${age}" -gt 150 ]; then\n        return 4\n    else\n        return 0\n    fi\n}`,
      hints: ['Use regex to check numeric.', 'Use -lt and -gt for range checks.', 'Return different codes for each case.'],
      concepts: ['exit-code', 'function', 'validation'],
    },
    {
      id: 'bash-exit-15',
      title: 'Fix set -e with Pipe',
      type: 'fix-bug',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Fix the issue where set -e does not catch failures in pipes.',
      skeleton: `#!/bin/bash\nset -e\n\n# Bug: if grep fails, the script continues\ncat /etc/passwd | grep "nonexistent" | wc -l\necho "This should not print if grep found nothing"`,
      solution: `#!/bin/bash\nset -e\nset -o pipefail\n\n# Fixed: pipefail catches failures in any pipe component\ncat /etc/passwd | grep "nonexistent" | wc -l\necho "This should not print if grep found nothing"`,
      hints: ['set -e only checks the last command in a pipe.', 'set -o pipefail makes pipes return the rightmost failure.', 'Add set -o pipefail.'],
      concepts: ['set-e', 'pipefail', 'error-handling'],
    },
    {
      id: 'bash-exit-16',
      title: 'Write Retry Function',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a retry function that retries a command up to N times, returning the last exit code.',
      skeleton: `# Write retry MAX_ATTEMPTS COMMAND...\n# Retry until success or max attempts reached`,
      solution: `retry() {\n    local max="\\\$1"\n    shift\n    local attempt=1\n    while [ "\\\${attempt}" -le "\\\${max}" ]; do\n        "\\\$@" && return 0\n        echo "Attempt \\\${attempt} failed" >&2\n        attempt=\\\$(( attempt + 1 ))\n    done\n    return 1\n}`,
      hints: ['Loop up to max attempts.', 'Run the command with "$@" and check success.', 'return 0 on success, return 1 after all retries fail.'],
      concepts: ['exit-code', 'function', 'retry-logic'],
    },
    {
      id: 'bash-exit-17',
      title: 'Signal Exit Codes',
      type: 'fill-blank',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Calculate the exit code when a process is killed by SIGTERM (signal 15).',
      skeleton: `# A process killed by signal N has exit code 128+N\n# SIGTERM is signal 15\n# Exit code for SIGTERM kill: ___`,
      solution: `# A process killed by signal N has exit code 128+N\n# SIGTERM is signal 15\n# Exit code for SIGTERM kill: 143`,
      hints: ['128 + signal number.', 'SIGTERM is signal 15.', '128 + 15 = 143'],
      concepts: ['exit-code', 'signals'],
    },
    {
      id: 'bash-exit-18',
      title: 'Refactor Nested Ifs to Exit Codes',
      type: 'refactor',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Refactor deeply nested if/else checks into early returns with exit codes.',
      skeleton: `deploy() {\n    if [ -f "config.yml" ]; then\n        if [ -d "build" ]; then\n            if [ -x "deploy.sh" ]; then\n                ./deploy.sh\n            else\n                echo "deploy.sh not executable" >&2\n            fi\n        else\n            echo "build dir missing" >&2\n        fi\n    else\n        echo "config.yml missing" >&2\n    fi\n}`,
      solution: `deploy() {\n    [ -f "config.yml" ] || { echo "config.yml missing" >&2; return 1; }\n    [ -d "build" ] || { echo "build dir missing" >&2; return 2; }\n    [ -x "deploy.sh" ] || { echo "deploy.sh not executable" >&2; return 3; }\n    ./deploy.sh\n}`,
      hints: ['Use guard clauses with || and return.', 'Each check returns a different exit code.', 'Flatten the nesting.'],
      concepts: ['exit-code', 'refactor', 'guard-clauses'],
    },
    {
      id: 'bash-exit-19',
      title: 'Refactor Error Handling Pattern',
      type: 'refactor',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Refactor repetitive error checking into a helper function.',
      skeleton: `mkdir -p /tmp/build\nif [ \\\$? -ne 0 ]; then echo "Failed to create dir" >&2; exit 1; fi\n\ncp src/* /tmp/build/\nif [ \\\$? -ne 0 ]; then echo "Failed to copy files" >&2; exit 1; fi\n\ntar czf build.tar.gz -C /tmp build\nif [ \\\$? -ne 0 ]; then echo "Failed to create archive" >&2; exit 1; fi`,
      solution: `run_or_die() {\n    "\\\$@" || { echo "Failed: \\\$*" >&2; exit 1; }\n}\n\nrun_or_die mkdir -p /tmp/build\nrun_or_die cp src/* /tmp/build/\nrun_or_die tar czf build.tar.gz -C /tmp build`,
      hints: ['Create a helper that runs a command and exits on failure.', 'Use "$@" to pass the command through.', 'Replace repetitive if/then blocks.'],
      concepts: ['exit-code', 'refactor', 'helper-function'],
    },
    {
      id: 'bash-exit-20',
      title: 'Write Exit Code Summary',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a function run_all that runs multiple commands, collects their exit codes, and prints a summary.',
      skeleton: `# Write run_all CMD1 CMD2 CMD3...\n# Run each, collect exit codes, print summary\n# Return 0 if all succeeded, 1 otherwise`,
      solution: `run_all() {\n    local -a codes=()\n    local all_ok=0\n    for cmd in "\\\$@"; do\n        eval "\\\${cmd}"\n        local code=\\\$?\n        codes+=("\\\${code}")\n        [ "\\\${code}" -ne 0 ] && all_ok=1\n    done\n    echo "Exit codes: \\\${codes[*]}"\n    return "\\\${all_ok}"\n}`,
      hints: ['Store each exit code in an array.', 'Track whether any failed.', 'Print all codes at the end.'],
      concepts: ['exit-code', 'arrays', 'function'],
    },
  ]
};