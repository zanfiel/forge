import type { LessonSection } from '../../../stores/app.svelte.ts';

export const section: LessonSection = {
  id: 'bash-cond',
  title: '05. Conditionals',
  explanation: `## Conditionals in Bash\n\nBash conditionals use **if/elif/else/fi**:\n\n\`\`\`bash\nif [ condition ]; then\n  echo "true"\nelif [ other ]; then\n  echo "other"\nelse\n  echo "false"\nfi\n\`\`\`\n\n**Test commands:**\n- \`[ ]\` is the classic test (POSIX)\n- \`[[ ]]\` is the Bash-enhanced test (supports regex, pattern matching, safer)\n\n**File tests:** \`-f\` (file exists), \`-d\` (directory), \`-e\` (exists), \`-r\` (readable), \`-w\` (writable), \`-x\` (executable)\n\n**String comparison:** \`=\` or \`==\`, \`!=\`, \`-z\` (empty), \`-n\` (non-empty)\n\n**Numeric comparison:** \`-eq\`, \`-ne\`, \`-lt\`, \`-gt\`, \`-le\`, \`-ge\`\n\n**Logical operators:**\n- Inside \`[[ ]]\`: \`&&\`, \`||\`, \`!\`\n- Inside \`[ ]\`: \`-a\`, \`-o\`, \`!\`\n- Between commands: \`&&\`, \`||\`\n\n**Case statement:**\n\`\`\`bash\ncase \$var in\n  pattern1) echo "match1" ;;\n  pattern2) echo "match2" ;;\n  *) echo "default" ;;\nesac\n\`\`\``,
  exercises: [
    {
      id: 'bash-cond-1',
      title: 'Basic If Statement',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Complete the if statement to check if x equals 5.',
      skeleton: `#!/bin/bash\nx=5\n___ [ \$x -eq 5 ]; ___\n  echo "Five!"\nfi`,
      solution: `#!/bin/bash\nx=5\nif [ \$x -eq 5 ]; then\n  echo "Five!"\nfi`,
      hints: ['if starts the conditional, then starts the body.', 'if [ condition ]; then is the syntax.', 'if ... then ... fi'],
      concepts: ['if-statement', 'numeric-comparison'],
    },
    {
      id: 'bash-cond-2',
      title: 'File Existence Test',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Check if the file /etc/passwd exists using the -f test.',
      skeleton: `#!/bin/bash\nif [ ___ /etc/passwd ]; then\n  echo "File exists"\nfi`,
      solution: `#!/bin/bash\nif [ -f /etc/passwd ]; then\n  echo "File exists"\nfi`,
      hints: ['-f tests if a regular file exists.', '-e tests any existence, -f is specifically for files.', '-f /etc/passwd'],
      concepts: ['file-tests'],
    },
    {
      id: 'bash-cond-3',
      title: 'String Comparison',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Compare two strings for equality using [[ ]].',
      skeleton: `#!/bin/bash\nlang="bash"\nif [[ \$lang ___ "bash" ]]; then\n  echo "Bash detected"\nfi`,
      solution: `#!/bin/bash\nlang="bash"\nif [[ \$lang == "bash" ]]; then\n  echo "Bash detected"\nfi`,
      hints: ['== is string equality in [[ ]].', 'Use == or = for string comparison.', '== "bash"'],
      concepts: ['string-comparison', 'double-bracket'],
    },
    {
      id: 'bash-cond-4',
      title: 'Numeric Greater Than',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Check if score is greater than 90 using numeric comparison.',
      skeleton: `#!/bin/bash\nscore=95\nif [ \$score ___ 90 ]; then\n  echo "Excellent!"\nfi`,
      solution: `#!/bin/bash\nscore=95\nif [ \$score -gt 90 ]; then\n  echo "Excellent!"\nfi`,
      hints: ['-gt means greater than for numbers.', 'Numeric comparisons use -eq, -ne, -lt, -gt, -le, -ge.', '-gt 90'],
      concepts: ['numeric-comparison'],
    },
    {
      id: 'bash-cond-5',
      title: 'If-Elif-Else Chain',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Complete the elif and else clauses.',
      skeleton: `#!/bin/bash\ngrade=75\nif [ \$grade -ge 90 ]; then\n  echo "A"\n___ [ \$grade -ge 80 ]; then\n  echo "B"\n___\n  echo "C"\nfi`,
      solution: `#!/bin/bash\ngrade=75\nif [ \$grade -ge 90 ]; then\n  echo "A"\nelif [ \$grade -ge 80 ]; then\n  echo "B"\nelse\n  echo "C"\nfi`,
      hints: ['elif is "else if" in Bash.', 'else handles the remaining case.', 'elif and else'],
      concepts: ['elif', 'else'],
    },
    {
      id: 'bash-cond-6',
      title: 'Logical AND in Condition',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Check if age is between 18 and 65 (inclusive) using && inside [[ ]].',
      skeleton: `#!/bin/bash\nage=30\nif [[ \$age -ge 18 ___ \$age -le 65 ]]; then\n  echo "Working age"\nfi`,
      solution: `#!/bin/bash\nage=30\nif [[ \$age -ge 18 && \$age -le 65 ]]; then\n  echo "Working age"\nfi`,
      hints: ['&& is logical AND inside [[ ]].', 'Both conditions must be true.', '&& $age -le 65'],
      concepts: ['logical-operators', 'double-bracket'],
    },
    {
      id: 'bash-cond-7',
      title: 'Write Case Statement',
      type: 'write-function',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Write a case statement that matches a variable fruit: "apple" prints "Red", "banana" prints "Yellow", anything else prints "Unknown".',
      skeleton: `#!/bin/bash\nfruit="apple"\n# Write case statement`,
      solution: `#!/bin/bash\nfruit="apple"\ncase \$fruit in\n  apple) echo "Red" ;;\n  banana) echo "Yellow" ;;\n  *) echo "Unknown" ;;\nesac`,
      hints: ['case $var in starts the statement.', 'Each pattern ends with ) and the clause ends with ;;', '* is the default pattern.'],
      concepts: ['case-statement'],
    },
    {
      id: 'bash-cond-8',
      title: 'Write Directory Check',
      type: 'write-function',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Write a script that checks if /tmp exists as a directory. Print "Found" or "Not found".',
      skeleton: `#!/bin/bash\n# Check if /tmp directory exists`,
      solution: `#!/bin/bash\nif [ -d /tmp ]; then\n  echo "Found"\nelse\n  echo "Not found"\nfi`,
      hints: ['-d tests for directory existence.', 'Use if/else for two outcomes.', 'if [ -d /tmp ]; then'],
      concepts: ['file-tests', 'if-statement'],
    },
    {
      id: 'bash-cond-9',
      title: 'Write Empty String Check',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function is_empty that returns 0 if the first argument is empty, 1 otherwise. Use -z test.',
      skeleton: `#!/bin/bash\n# Write is_empty function`,
      solution: `#!/bin/bash\nis_empty() {\n  if [ -z "\$1" ]; then\n    return 0\n  else\n    return 1\n  fi\n}`,
      hints: ['-z tests if a string is empty (zero length).', 'return 0 means true/success in Bash.', '[ -z "$1" ]'],
      concepts: ['string-tests', 'functions', 'return-codes'],
    },
    {
      id: 'bash-cond-10',
      title: 'Write Grade Calculator',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function get_grade that takes a numeric score and echoes the letter grade: >=90 A, >=80 B, >=70 C, >=60 D, else F.',
      skeleton: `#!/bin/bash\n# Write get_grade function`,
      solution: `#!/bin/bash\nget_grade() {\n  if [ \$1 -ge 90 ]; then\n    echo "A"\n  elif [ \$1 -ge 80 ]; then\n    echo "B"\n  elif [ \$1 -ge 70 ]; then\n    echo "C"\n  elif [ \$1 -ge 60 ]; then\n    echo "D"\n  else\n    echo "F"\n  fi\n}`,
      hints: ['Use if/elif/else chain with -ge.', 'Check from highest to lowest.', '$1 is the function argument.'],
      concepts: ['if-statement', 'elif', 'numeric-comparison'],
    },
    {
      id: 'bash-cond-11',
      title: 'Write File Type Checker',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function check_path that takes a path and prints "file", "directory", or "not found" using -f and -d tests.',
      skeleton: `#!/bin/bash\n# Write check_path function`,
      solution: `#!/bin/bash\ncheck_path() {\n  if [ -f "\$1" ]; then\n    echo "file"\n  elif [ -d "\$1" ]; then\n    echo "directory"\n  else\n    echo "not found"\n  fi\n}`,
      hints: ['-f tests for regular file, -d for directory.', 'Use elif to chain the tests.', 'Quote "$1" for paths with spaces.'],
      concepts: ['file-tests', 'if-statement'],
    },
    {
      id: 'bash-cond-12',
      title: 'Write OR Condition',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a script that checks if the variable day is "Saturday" or "Sunday" and prints "Weekend!" or "Weekday".',
      skeleton: `#!/bin/bash\nday="Saturday"\n# Check for weekend`,
      solution: `#!/bin/bash\nday="Saturday"\nif [[ \$day == "Saturday" || \$day == "Sunday" ]]; then\n  echo "Weekend!"\nelse\n  echo "Weekday"\nfi`,
      hints: ['|| is logical OR inside [[ ]].', 'Check both weekend days.', '[[ $day == "Saturday" || $day == "Sunday" ]]'],
      concepts: ['logical-operators', 'string-comparison'],
    },
    {
      id: 'bash-cond-13',
      title: 'Predict If Output',
      type: 'predict-output',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Predict the output of this conditional.',
      skeleton: `#!/bin/bash\nx=10\nif [ \$x -gt 5 ]; then\n  echo "big"\nelse\n  echo "small"\nfi`,
      solution: `big`,
      hints: ['10 is greater than 5.', '-gt means greater than.', 'The if branch executes.'],
      concepts: ['numeric-comparison', 'if-statement'],
    },
    {
      id: 'bash-cond-14',
      title: 'Predict Case Output',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Predict the output of this case statement.',
      skeleton: `#!/bin/bash\nval="hello"\ncase \$val in\n  h*) echo "starts with h" ;;\n  *o) echo "ends with o" ;;\n  *) echo "other" ;;\nesac`,
      solution: `starts with h`,
      hints: ['Case matches the first matching pattern.', 'h* matches any string starting with h.', '"hello" starts with h, so first pattern matches.'],
      concepts: ['case-statement', 'pattern-matching'],
    },
    {
      id: 'bash-cond-15',
      title: 'Predict Short-Circuit',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Predict the output of this short-circuit evaluation.',
      skeleton: `#!/bin/bash\n[ 1 -eq 1 ] && echo "yes" || echo "no"\n[ 1 -eq 2 ] && echo "yes" || echo "no"`,
      solution: `yes\nno`,
      hints: ['&& runs the next command only if the previous succeeded.', '|| runs the next command only if the previous failed.', '1 -eq 1 is true; 1 -eq 2 is false.'],
      concepts: ['short-circuit', 'logical-operators'],
    },
    {
      id: 'bash-cond-16',
      title: 'Fix Missing Spaces in Test',
      type: 'fix-bug',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Fix the test command that is missing required spaces around brackets.',
      skeleton: `#!/bin/bash\nx=5\nif [\$x -eq 5]; then\n  echo "five"\nfi`,
      solution: `#!/bin/bash\nx=5\nif [ \$x -eq 5 ]; then\n  echo "five"\nfi`,
      hints: ['[ is a command and needs spaces around it.', 'Space after [ and before ] are required.', '[ $x -eq 5 ]'],
      concepts: ['test-command'],
    },
    {
      id: 'bash-cond-17',
      title: 'Fix String vs Numeric Compare',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Fix the comparison that uses string operators for numbers.',
      skeleton: `#!/bin/bash\na=10\nb=9\nif [ \$a > \$b ]; then\n  echo "\$a is bigger"\nfi`,
      solution: `#!/bin/bash\na=10\nb=9\nif [ \$a -gt \$b ]; then\n  echo "\$a is bigger"\nfi`,
      hints: ['> in [ ] does file redirection, not comparison.', 'Use -gt for numeric greater-than.', '[ $a -gt $b ]'],
      concepts: ['numeric-comparison'],
    },
    {
      id: 'bash-cond-18',
      title: 'Fix Unquoted Variable in Test',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Fix the test that breaks when the variable is empty.',
      skeleton: `#!/bin/bash\nname=""\nif [ \$name = "admin" ]; then\n  echo "admin"\nfi`,
      solution: `#!/bin/bash\nname=""\nif [ "\$name" = "admin" ]; then\n  echo "admin"\nfi`,
      hints: ['Empty unquoted variable causes syntax error in [ ].', 'Always quote variables in [ ] tests.', '[ "$name" = "admin" ]'],
      concepts: ['quoting', 'test-command'],
    },
    {
      id: 'bash-cond-19',
      title: 'Refactor If-Else to Case',
      type: 'refactor',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Refactor this if/elif chain into a case statement.',
      skeleton: `#!/bin/bash\next="\$1"\nif [ "\$ext" = "txt" ]; then\n  echo "Text file"\nelif [ "\$ext" = "sh" ]; then\n  echo "Shell script"\nelif [ "\$ext" = "py" ]; then\n  echo "Python script"\nelse\n  echo "Unknown type"\nfi`,
      solution: `#!/bin/bash\next="\$1"\ncase \$ext in\n  txt) echo "Text file" ;;\n  sh) echo "Shell script" ;;\n  py) echo "Python script" ;;\n  *) echo "Unknown type" ;;\nesac`,
      hints: ['case is cleaner for matching a single variable.', 'Each pattern uses ) and ends with ;;', '* is the default case.'],
      concepts: ['case-statement', 'refactoring'],
    },
    {
      id: 'bash-cond-20',
      title: 'Refactor to [[ ]]',
      type: 'refactor',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Refactor this script from [ ] with -a/-o to [[ ]] with &&/||.',
      skeleton: `#!/bin/bash\nage=25\nname="Alice"\nif [ \$age -ge 18 -a "\$name" != "" ]; then\n  echo "Valid adult user"\nfi\nif [ "\$name" = "admin" -o "\$name" = "root" ]; then\n  echo "Superuser"\nfi`,
      solution: `#!/bin/bash\nage=25\nname="Alice"\nif [[ \$age -ge 18 && -n \$name ]]; then\n  echo "Valid adult user"\nfi\nif [[ \$name == "admin" || \$name == "root" ]]; then\n  echo "Superuser"\nfi`,
      hints: ['[[ ]] supports && and || directly.', '-a becomes &&, -o becomes ||.', '[[ ]] is safer and more feature-rich.'],
      concepts: ['double-bracket', 'logical-operators', 'refactoring'],
    },
  ]
};
