import type { LessonSection } from '../../../stores/app.svelte.ts';

export const section: LessonSection = {
  id: 'bash-admin',
  title: '41. System Administration',
  explanation: `## System Administration\n\nBash is the primary tool for Linux system administration. Common tasks include managing users, services, disks, and system resources.\n\n\`\`\`bash\n# User management\nuseradd -m -s /bin/bash newuser\npasswd newuser\nusermod -aG sudo newuser\n\n# Service control\nsystemctl start nginx\nsystemctl enable nginx\nsystemctl status nginx\n\n# Disk usage\ndf -h          # filesystem usage\ndu -sh /var/*  # directory sizes\n\n# System info\nfree -h        # memory usage\nlscpu          # CPU information\nuname -a       # kernel info\n\`\`\`\n\n**Key areas:**\n\n- **User management:** useradd, usermod, userdel, passwd, groups\n- **Service control:** systemctl start/stop/restart/enable/disable/status\n- **Disk:** df, du, lsblk, mount, fdisk\n- **Memory:** free, /proc/meminfo\n- **CPU:** lscpu, /proc/cpuinfo, top, htop\n- **Logs:** journalctl, /var/log/\n- **Cron:** crontab -e, /etc/crontab\n- **Packages:** apt/yum/dnf install/remove/update\n- **Network config:** ip addr, hostname, /etc/hosts\n- **Monitoring:** top, ps, uptime, vmstat`,
  exercises: [
    {
      id: 'bash-admin-1',
      title: 'Create a New User',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Create a new user called "deploy" with a home directory and bash shell.',
      skeleton: `#!/bin/bash\n___ -m -s /bin/bash deploy`,
      solution: `#!/bin/bash\nuseradd -m -s /bin/bash deploy`,
      hints: ['useradd creates new users.', '-m creates the home directory.', '-s sets the login shell.'],
      concepts: ['useradd', 'user-management'],
    },
    {
      id: 'bash-admin-2',
      title: 'Add User to Group',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Add user "deploy" to the "docker" group without removing other group memberships.',
      skeleton: `#!/bin/bash\nusermod ___ docker deploy`,
      solution: `#!/bin/bash\nusermod -aG docker deploy`,
      hints: ['-a means append (do not remove from other groups).', '-G specifies the supplementary group.', 'usermod -aG GROUP USER'],
      concepts: ['usermod', 'groups'],
    },
    {
      id: 'bash-admin-3',
      title: 'Start and Enable a Service',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Start the nginx service and enable it to start on boot.',
      skeleton: `#!/bin/bash\nsystemctl ___ nginx\nsystemctl ___ nginx`,
      solution: `#!/bin/bash\nsystemctl start nginx\nsystemctl enable nginx`,
      hints: ['start begins the service immediately.', 'enable makes it start on boot.', 'systemctl start/enable SERVICE'],
      concepts: ['systemctl', 'services'],
    },
    {
      id: 'bash-admin-4',
      title: 'Check Disk Usage',
      type: 'fill-blank',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Show filesystem disk usage in human-readable format.',
      skeleton: `#!/bin/bash\n___ -h`,
      solution: `#!/bin/bash\ndf -h`,
      hints: ['df shows disk filesystem usage.', '-h makes it human-readable (GB, MB).', 'df -h'],
      concepts: ['df', 'disk-usage'],
    },
    {
      id: 'bash-admin-5',
      title: 'View Recent System Logs',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'View the last 50 lines of systemd journal logs for the sshd service.',
      skeleton: `#!/bin/bash\n___ -u sshd -n 50`,
      solution: `#!/bin/bash\njournalctl -u sshd -n 50`,
      hints: ['journalctl reads systemd logs.', '-u filters by unit/service.', '-n limits the number of lines.'],
      concepts: ['journalctl', 'system-logs'],
    },
    {
      id: 'bash-admin-6',
      title: 'Schedule a Cron Job',
      type: 'fill-blank',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a cron expression that runs a backup script every day at 2:30 AM.',
      skeleton: `# crontab entry\n___ /opt/scripts/backup.sh`,
      solution: `# crontab entry\n30 2 * * * /opt/scripts/backup.sh`,
      hints: ['Cron format: minute hour day month weekday.', '30 2 means 2:30 AM.', '* * * means every day, every month, every weekday.'],
      concepts: ['cron', 'scheduling'],
    },
    {
      id: 'bash-admin-7',
      title: 'Write a User Setup Script',
      type: 'write-function',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Write a function setup_user that takes a username, creates the user with a home directory and bash shell, and adds them to the sudo group.',
      skeleton: `#!/bin/bash\n# Write setup_user function`,
      solution: `#!/bin/bash\nsetup_user() {\n  local user=\$1\n  useradd -m -s /bin/bash "\$user"\n  usermod -aG sudo "\$user"\n  echo "User \$user created and added to sudo"\n}`,
      hints: ['Use useradd -m -s /bin/bash for creation.', 'Use usermod -aG sudo for group membership.', 'Take the username as a parameter.'],
      concepts: ['useradd', 'usermod', 'functions'],
    },
    {
      id: 'bash-admin-8',
      title: 'Write a Service Status Checker',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function check_services that takes a list of service names and prints whether each is active or inactive.',
      skeleton: `#!/bin/bash\n# Write check_services function`,
      solution: `#!/bin/bash\ncheck_services() {\n  for service in "\$@"; do\n    if systemctl is-active --quiet "\$service"; then\n      echo "\$service: active"\n    else\n      echo "\$service: inactive"\n    fi\n  done\n}`,
      hints: ['systemctl is-active checks if a service is running.', '--quiet suppresses output, just sets exit code.', 'Loop over "$@" for all arguments.'],
      concepts: ['systemctl', 'functions', 'loops'],
    },
    {
      id: 'bash-admin-9',
      title: 'Write a Disk Space Report',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function disk_report that shows the top 5 directories by size under a given path using du.',
      skeleton: `#!/bin/bash\n# Write disk_report function`,
      solution: `#!/bin/bash\ndisk_report() {\n  local path=\${1:-.}\n  echo "Top 5 directories by size under \$path:"\n  du -sh "\$path"/* 2>/dev/null | sort -rh | head -5\n}`,
      hints: ['du -sh gives human-readable sizes.', 'sort -rh sorts by human-readable numbers in reverse.', 'head -5 limits to top 5.'],
      concepts: ['du', 'sort', 'head', 'functions'],
    },
    {
      id: 'bash-admin-10',
      title: 'Write a Memory Usage Function',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function mem_usage that parses free -m output to show total, used, and available memory in MB.',
      skeleton: `#!/bin/bash\n# Write mem_usage function`,
      solution: `#!/bin/bash\nmem_usage() {\n  local total used available\n  read -r total used available <<< \$(free -m | awk '/^Mem:/ {print \$2, \$3, \$7}')\n  echo "Total: \${total}MB"\n  echo "Used: \${used}MB"\n  echo "Available: \${available}MB"\n}`,
      hints: ['free -m shows memory in megabytes.', 'awk can extract specific fields from the Mem: line.', 'Field 2=total, 3=used, 7=available.'],
      concepts: ['free', 'awk', 'memory'],
    },
    {
      id: 'bash-admin-11',
      title: 'Write a System Info Report',
      type: 'write-function',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Write a function sys_info that prints hostname, kernel version, CPU model, total memory, and disk usage percentage of /.',
      skeleton: `#!/bin/bash\n# Write sys_info function`,
      solution: `#!/bin/bash\nsys_info() {\n  echo "Hostname: \$(hostname)"\n  echo "Kernel: \$(uname -r)"\n  echo "CPU: \$(lscpu | awk -F: '/Model name/ {gsub(/^[ \\t]+/,\"\",\$2); print \$2}')"\n  echo "Memory: \$(free -h | awk '/^Mem:/ {print \$2}') total"\n  echo "Disk /: \$(df -h / | awk 'NR==2 {print \$5}') used"\n}`,
      hints: ['hostname, uname -r, lscpu, free -h, df -h are the key commands.', 'Use awk to extract specific fields.', 'df -h / shows root filesystem usage.'],
      concepts: ['hostname', 'uname', 'lscpu', 'free', 'df'],
    },
    {
      id: 'bash-admin-12',
      title: 'Write Package Install Script',
      type: 'write-function',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Write a function install_packages that detects whether the system uses apt or dnf and installs a list of packages accordingly.',
      skeleton: `#!/bin/bash\n# Write install_packages function`,
      solution: `#!/bin/bash\ninstall_packages() {\n  local pkg_manager\n  if command -v apt &>/dev/null; then\n    pkg_manager="apt install -y"\n  elif command -v dnf &>/dev/null; then\n    pkg_manager="dnf install -y"\n  else\n    echo "Unsupported package manager" >&2\n    return 1\n  fi\n  \$pkg_manager "\$@"\n}`,
      hints: ['Use command -v to detect available package managers.', 'apt for Debian/Ubuntu, dnf for Fedora/RHEL.', 'Pass "$@" for the list of packages.'],
      concepts: ['package-management', 'apt', 'dnf', 'portability'],
    },
    {
      id: 'bash-admin-13',
      title: 'Predict df Output Fields',
      type: 'predict-output',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'How many columns does df output have?',
      skeleton: `#!/bin/bash\necho "Filesystem 1K-blocks Used Available Use% Mounted" | wc -w`,
      solution: `6`,
      hints: ['Count the words in the df header.', 'Filesystem, 1K-blocks, Used, Available, Use%, Mounted.', 'wc -w counts 6 words.'],
      concepts: ['df', 'wc'],
    },
    {
      id: 'bash-admin-14',
      title: 'Predict cron Schedule',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'How often does this cron job run?',
      skeleton: `#!/bin/bash\n# Cron: */15 * * * * /script.sh\necho "Every 15 minutes" | cat`,
      solution: `Every 15 minutes`,
      hints: ['*/15 in the minute field means every 15 minutes.', 'The other fields are all * (any).', 'So it runs every 15 minutes.'],
      concepts: ['cron', 'scheduling'],
    },
    {
      id: 'bash-admin-15',
      title: 'Predict systemctl Check',
      type: 'predict-output',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'What does systemctl is-active output for a running service?',
      skeleton: `#!/bin/bash\necho "active"`,
      solution: `active`,
      hints: ['is-active prints "active" for running services.', 'It prints "inactive" for stopped services.', 'The output is the string "active".'],
      concepts: ['systemctl', 'services'],
    },
    {
      id: 'bash-admin-16',
      title: 'Fix useradd Missing Home',
      type: 'fix-bug',
      difficulty: 'beginner',
      language: 'bash',
      goal: 'Fix this user creation that fails to create a home directory.',
      skeleton: `#!/bin/bash\nuseradd -s /bin/bash newuser`,
      solution: `#!/bin/bash\nuseradd -m -s /bin/bash newuser`,
      hints: ['Without -m, no home directory is created.', '-m creates /home/username.', 'Add -m flag.'],
      concepts: ['useradd', 'home-directory'],
    },
    {
      id: 'bash-admin-17',
      title: 'Fix Wrong systemctl Command',
      type: 'fix-bug',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Fix this script that tries to check service status but uses the wrong systemctl subcommand.',
      skeleton: `#!/bin/bash\nif systemctl running nginx; then\n  echo "nginx is running"\nfi`,
      solution: `#!/bin/bash\nif systemctl is-active --quiet nginx; then\n  echo "nginx is running"\nfi`,
      hints: ['There is no "running" subcommand.', 'Use "is-active" to check if a service is running.', 'Add --quiet to suppress output.'],
      concepts: ['systemctl', 'is-active'],
    },
    {
      id: 'bash-admin-18',
      title: 'Fix Cron Environment Issue',
      type: 'fix-bug',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Fix this cron script that fails because commands are not found in the cron environment.',
      skeleton: `#!/bin/bash\n# Cron entry: * * * * * /opt/scripts/check.sh\nnginx -t\ncurl -s http://localhost/health`,
      solution: `#!/bin/bash\n# Cron entry: * * * * * /opt/scripts/check.sh\nexport PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\n/usr/sbin/nginx -t\n/usr/bin/curl -s http://localhost/health`,
      hints: ['Cron has a minimal PATH.', 'Either set PATH explicitly or use full paths.', 'export PATH=... at the top of the script.'],
      concepts: ['cron', 'path', 'environment'],
    },
    {
      id: 'bash-admin-19',
      title: 'Refactor Inline Admin Commands',
      type: 'refactor',
      difficulty: 'intermediate',
      language: 'bash',
      goal: 'Refactor these inline commands into a structured setup_server function with error checking.',
      skeleton: `#!/bin/bash\nuseradd -m -s /bin/bash deploy\nusermod -aG sudo deploy\napt update\napt install -y nginx\nsystemctl start nginx\nsystemctl enable nginx`,
      solution: `#!/bin/bash\nsetup_server() {\n  echo "Creating user..."\n  useradd -m -s /bin/bash deploy || { echo "User creation failed"; return 1; }\n  usermod -aG sudo deploy\n\n  echo "Installing nginx..."\n  apt update && apt install -y nginx || { echo "Install failed"; return 1; }\n\n  echo "Starting nginx..."\n  systemctl start nginx && systemctl enable nginx || { echo "Service setup failed"; return 1; }\n\n  echo "Server setup complete"\n}\n\nsetup_server`,
      hints: ['Wrap commands in a function for reusability.', 'Add error checking with || { echo msg; return 1; }.', 'Add progress messages with echo.'],
      concepts: ['functions', 'error-handling', 'refactoring'],
    },
    {
      id: 'bash-admin-20',
      title: 'Refactor Repeated Status Checks',
      type: 'refactor',
      difficulty: 'advanced',
      language: 'bash',
      goal: 'Refactor these repeated service status checks into a loop with formatted output.',
      skeleton: `#!/bin/bash\necho -n "nginx: "\nsystemctl is-active nginx\necho -n "postgresql: "\nsystemctl is-active postgresql\necho -n "redis: "\nsystemctl is-active redis\necho -n "docker: "\nsystemctl is-active docker`,
      solution: `#!/bin/bash\nservices=(nginx postgresql redis docker)\nfor svc in "\${services[@]}"; do\n  status=\$(systemctl is-active "\$svc" 2>/dev/null)\n  printf "%-15s %s\\n" "\$svc:" "\$status"\ndone`,
      hints: ['Store service names in an array.', 'Loop and call systemctl for each.', 'Use printf for aligned output.'],
      concepts: ['systemctl', 'arrays', 'loops', 'printf'],
    },
  ]
};
